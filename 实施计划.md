# bollfile — 点对点消息与文件传输助手 实施计划

> 阶段目标：本阶段为**资料准备与方案设计**，不写业务代码。先调研 `/home/fei/workspace/bolloon` 的 P2P 实现，提炼可复用能力，确定 bollfile 的零配置自动发现方案、传输选型、协议设计与分阶段落地路线。
>
> 用户场景：两台**公网**电脑，通过一个对话框互相发**文本消息**和**文件**，**不需要用户做任何网络配置**（不填 IP、不配端口、不搭服务器）。
>
> **已确认约束**：① 使用 **Electron** 桌面端；② 走**自动广播/发现**机制，零配置。

---

## 0. 结论速览（先给答案）

- **零配置自动发现靠 hyperswarm topic**：bolloon 的 `p2p-direct.ts` + `auto-peer-discovery.ts` 已实现"加入同一 topic → 公共 DHT 自动找到对方 → 自动加好友"。hyperswarm 内置公共 DHT bootstrap，**开箱即用、全球可达、无需任何配置**。这是满足"不需用户配置"的唯一现成机制。
- **传输也选 hyperswarm**：它建立的 `conn` 是**全双工字节流**，我们在其上做长度帧（length-prefix）封装，即可同时承载聊天消息和大文件，**不受 64KB 限制**（iroh 才有 64KB 硬限）。所以一台机器、一份依赖即可搞定发现+传输。
- libp2p / iroh 在 bolloon 里都需要 bootstrap 节点 / 中央 DIAP 注册 / relay 配置，**不适合"零配置"**，降级为"后续可选增强"（仅在需要穿透对称 NAT/做中继时再加）。
- **最大缺口（bollfile 自补）**：现有代码没有"文件分块/重组/校验/续传"协议，也没有对话框 UI。这些属于本次要新建的增量。

---

## 1. 关键技术调研结论（来自 bolloon 真实代码）

### 1.1 机制 A：libp2p（`src/network/p2p.ts`）
| 项 | 结论 |
|---|---|
| 传输 | TCP + WebSocket，noise 加密 + yamux |
| NAT/中继 | circuit-relay-v2、autoNAT、UPnP、dcutr |
| 发现 | kad-dht，但 **bootstrapPeers / relayPeers 默认空**，不配置就无法自动发现 |
| 流 | `/agent/message` 按流累加，**无 64KB 上限** → 可传大文件 |
| 离线 | `storeOfflineMessage` + `peer-store.json` + 重连重投 |

**对本项目的价值**：仅作为"后续可选增强"（对称 NAT / 中继场景），零配置阶段不依赖它。

### 1.2 机制 B：iroh（`src/network/iroh-transport.ts`）
| 项 | 结论 |
|---|---|
| 依赖 | `@rayhanadev/iroh`（QUIC），ALPN `bolloon/iroh/1` |
| 发现 | 靠 `iroh-bootstrap.ts` 向**中央 DIAP 服务**注册才能被发现 → 非自包含、非零配置 |
| 致命限制 | `recv.readToEnd(64 * 1024)`，**单条消息最大 64KB** |
| 离线 | JSON 离线队列，retry=10 |

**对本项目的价值**：零配置阶段不采用（需中央注册 + 64KB 分块）。可作未来大文件 QUIC 优化。

### 1.3 机制 C：hyperswarm（`src/network/p2p-direct.ts`）★ 本项目主选
| 项 | 结论 |
|---|---|
| 依赖 | `hyperswarm`（纯 TS DHT）+ `b4a` |
| **发现** | `swarm.join(topic, {server:true, client:true})` 加入 topic；**内置公共 DHT bootstrap，零配置全球可达** |
| **自动加好友** | `auto-peer-discovery.ts`：连接建立后 `tryAutoDiscoverPeer` 自动写入 `known-peers`，无需用户操作 |
| 连接 | 全双工 `conn`（`conn.write` / `conn.on('data')`），基于 Noise 加密的 TCP 流 |
| 身份 | 每 role 持久 keyPair（`~/.bolloon/p2p-direct-secret-{role}.json`），跨重启稳定 |
| 安全 | `BOLLOON_P2P_INBOUND=0` 可拒所有入站；`input-scanner` 不可信输入扫描 |
| 主动单发 | `sendToWithWait(pk, data, timeout)`：先 `joinPeer` 触发握手，等到 conn 出现再写，避免"消息进虚空" |

**对本项目的价值**：发现 + 传输一把梭，**唯一满足零配置要求**的机制。

### 1.4 统一调度层（可借鉴）
`hybrid-messenger.ts` 抽象了 `selectTransport(type, size, priority)` 按内容选通道。bollfile 可直接用 hyperswarm 单通道 + 自研帧协议，不必照搬三通道切换。

### 1.5 ★ 自动发现 / 零配置广播机制（本次重点研究结论）
bolloon 已经跑通"零配置互连"的完整范式，bollfile 可直接复用：

1. **启动即发现**：`P2PDirect.start()` → `swarm.listen()`；`joinTopic('bolloon-agent-harness')` 加入公共 DHT topic。
2. **DHT 自动撮合**：两台机器加入**同一 topic**，hyperswarm 公共 DHT 自动把双方连起来，触发 `connection` 事件（`p2p-direct.ts:143`）。
3. **自动加好友**：`auto-peer-discovery.ts` 的 `tryAutoDiscoverPeer` 在连接建立后把远端 publicKey 自动写入 `known-peers`，无需用户点"添加"。
4. **自动广播**：`source-intent-broadcaster.ts` 示范了"同一 topic 内广播带前缀的 JSON 消息"。bollfile 可用同样方式广播 `presence`（在线/昵称/能力）和聊天/文件信令。

**隐私与"只让两台机器互联"的做法（关键设计）**：
- 公共 DHT 的 topic 是公开的，任何人加同一 topic 都会互相看见。
- 解决方法：**用"房间码 / 邀请链接"派生 topic**。用户 A 生成一个随机 `roomCode`（如 `bollfile-<32hex>`），双方输入同一个 `roomCode` → 派生出同一个私密 topic（`sha256(roomCode)` 的前若干字节）→ 只有知道该码的两台机器才会互相发现。
- 这样用户**零网络配置**（不碰 IP/端口），只需两台机器填同一个"房间码"即可自动互连。输入框里的"房间码"是产品层概念，不是网络配置。

---

## 2. 传输选型决策（针对"两台公网电脑 + 零配置"）

| 维度 | hyperswarm（主选） | libp2p（可选增强） | iroh（可选增强） |
|---|---|---|---|
| 自动发现 | ✅ 公共 DHT，零配置 | ❌ 需 bootstrap 节点 | ❌ 需中央 DIAP |
| 公网直连 | ✅ 直接 TCP 流 | ✅ 直连 | ✅ QUIC |
| 大文件 | ✅ 自研帧，无上限 | ✅ 流无上限 | ⚠️ 64KB 需分块 |
| 用户配置 | 仅填"房间码" | 需 IP/端口/bootstrap | 需注册中心 |
| 依赖复杂度 | 低（1 个包） | 高（10+ 包） | 中 |

**bollfile 默认策略**：
- **唯一主传输 = hyperswarm**：既做自动发现，又做消息/文件传输（上叠长度帧协议）。
- libp2p / iroh 列入"P6+ 可选增强"，仅在出现对称 NAT、需要中继或追求极致大文件吞吐时引入，**不影响零配置主线**。

---

## 3. 系统架构（bollfile）

```
┌──────────────────────────────────────────────┐
│  UI 层 (Electron 对话框)  — React 19           │
│   房间码输入 │ 消息气泡 │ 文件卡片+进度条 │ 历史  │
└───────────────┬──────────────────────────────┘
                │ IPC
┌───────────────▼──────────────────────────────┐
│  应用协议层 (bollfile 新建，核心增量)          │
│   ChatProtocol: presence/msg/ack/read/历史     │
│   FileProtocol: offer/accept/chunk/done/resume │
│   Framing: length-prefix 帧封装 (解决粘包)     │
└───────────────┬──────────────────────────────┘
                │
┌───────────────▼──────────────────────────────┐
│  传输层 (单通道)                               │
│   HyperswarmTransport:                         │
│    joinRoom(roomCode)→派生私密topic→自动发现    │
│    send(pk, bytes) / onData(handler)           │
│    sendToWithWait 保证握手完成再发              │
└───────────────┬──────────────────────────────┘
                │
┌───────────────▼──────────────────────────────┐
│  hyperswarm (公共 DHT, 零配置)                 │
│   topic = sha256(roomCode)   conn=全双工流     │
└──────────────────────────────────────────────┘
```

### 3.1 HyperswarmTransport 封装（bollfile 新建，复用 bolloon 写法）
```ts
class HyperswarmTransport {
  joinRoom(roomCode: string): void {        // 派生私密 topic，自动发现对方
    const topic = sha256(roomCode).slice(0, 32);
    this.swarm.join(Buffer.from(topic, 'hex'), { server: true, client: true });
  }
  onData(h: (bytes: Buffer, fromPk: string) => void): void;  // conn.on('data') 透传
  async sendTo(pk: string, bytes: Buffer): Promise<'SENT'|'NO_CONN'>;  // 用 sendToWithWait 范式
  getPublicKey(): string;                    // 本机稳定身份 (role 持久化)
}
```
- 复用 bolloon 的 `sendToWithWait`（先 `joinPeer` 触发握手、等 conn 出现再写）避免消息丢失。
- 复用 `input-scanner` 做不可信输入扫描（防远端恶意字节），保持 silence-on-fail 风格。

---

## 4. 协议设计（bollfile 核心增量，现有代码没有）

> 所有内容都跑在 hyperswarm 的**字节流**上，因此**没有 64KB 限制**，但必须自己解决"粘包"——采用 **长度前缀帧**：`[4字节大端长度][payload]`。

### 4.1 帧格式（Framing）
```
帧 = uint32BE(length) || payload
payload 内是 JSON（控制信令）或 二进制（文件块，带内联头）
```
收端循环 `read 4 字节长度 → read N 字节 payload → 解析`，天然支持任意大小消息与文件流。

### 4.2 聊天消息协议（文本 + 信令）
```
presence 广播(topic内): { t:'presence', name, caps:['chat','file'], pk }
chat.msg:  { t:'chat.msg', id, from, to, text, ts, sig }
chat.ack:  { t:'chat.ack', id }            // 已送达
chat.read: { t:'chat.read', id }           // 已读
```
- `sig`：用本机持久 keyPair 对 `sha256(text+ts)` 签名（复用 bolloon `keypair` 思路）。
- 历史：本地 `~/.bollfile/history/<peer>.jsonl`，双方各存一份。

### 4.3 文件传输协议（关键，现有完全缺失）
采用**清单 + 分块 + 校验 + 续传**（因 hyperswarm 是流，分块仅用于校验/续传，不卡 64KB）：
```
(1) offer:  { t:'file.offer', id, name, size, mime, totalChunks, sha256, chunkSize }
(2) accept/reject: { t:'file.accept' } / { t:'file.reject' }
(3) 逐块:   { t:'file.chunk', id, index, sha256 } || <二进制块>   // 块直接跟在 JSON 头之后
(4) chunk.ack: { t:'file.chunk.ack', id, index }
(5) done:   { t:'file.done', id } → 收端校验整文件 sha256 == offer.sha256
(6) resume: 断线重连后 { t:'file.resume', id } → 回已收最大 index → 续传
```
- 块大小建议 256KB~1MB（hyperswarm 流可承压，过大不利续传粒度）。
- 进度：UI 实时显示 已传/总数、速度、ETA。

---

## 5. 分阶段实施路线

| 阶段 | 目标 | 产出 | 依赖 |
|---|---|---|---|
| **P0 资料准备** | 读完三机制 + 确定零配置方案 + 搭空项目 | 本计划 + 空仓库骨架 | — |
| **P1 传输+发现** | `HyperswarmTransport`：房间码派生 topic、自动发现、帧封装、`sendToWithWait` | 两机填同房间码即互发 "hello" | 复制 hyperswarm 依赖 |
| **P2 聊天消息** | presence 广播 + ChatProtocol + 历史落盘 + 回执 | 对话框能收发文本、自动显示对方在线 | P1 |
| **P3 文件协议** | FileProtocol：offer/accept/chunk/done/resume + sha256 | 能传任意大小文件、可续传 | P1 |
| **P4 对话框 UI** | Electron 桌面：输入框 + 文件拖拽 + 进度条 + 历史 + 房间码页 | 可用桌面助手 | P2,P3 |
| **P5 身份与配对** | 稳定 keyPair + 签名验签 + 昵称 | 防伪造、显示对方昵称 | P2 |
| **P6 可选增强** | libp2p/iroh 兜底（对称 NAT/中继/大文件优化） | 极端网络也能连 | P1 |

---

## 6. UI 形态（✅ 已确认用 Electron）

bolloon 本身就是 **Electron + React 19 + express**（`package.json`：`electron ^44`、`react ^19`、`express ^5`）。bollfile 复用同栈：
- **主进程**：起 hyperswarm 节点 + 本地 `express`（静态资源/REST 给渲染进程调用）。
- **渲染进程**：React 对话框组件（房间码页、消息气泡、文件卡片、进度条、拖拽上传）。
- **通信**：主进程 ↔ 渲染进程用 Electron IPC（或 express + WebSocket）。
- 首屏 = "房间码"输入/生成页；进入后 = 对话框。

---

## 7. 资料准备清单（当前阶段可做）

1. **依赖（精简，仅主选）**：
   - `hyperswarm` + `b4a`（核心，零配置发现+传输）
   - 可选复用：`@bolloon/.../security/input-scanner`（不可信输入扫描）
   - UI：`electron@^44`、`react@^19`、`express@^5`
   - （libp2p / iroh 依赖**暂不引入**，留 P6）
2. **直接复用的 bolloon 源码范式**：
   - `src/network/p2p-direct.ts`：`start/joinTopic/sendToWithWait/getPublicKey`（自动发现+可靠单发）
   - `src/network/auto-peer-discovery.ts`：`tryAutoDiscoverPeer`（连接即自动加好友）
   - `src/network/source-intent-broadcaster.ts`：topic 内广播带前缀 JSON 消息的写法
   - `src/network/p2p-secret.ts`：role 持久 keyPair（稳定身份，跨重启）
   - `src/security/input-scanner.ts`：入站字节安全扫描
3. **概念验证（P1 验证项）**：
   - 两台公网机器填**同一房间码** → 是否经公共 DHT 自动互连（验证零配置成立）。
   - hyperswarm `conn` 上叠加 length-prefix 帧后，>1MB 文件是否稳定不粘包。
   - `sendToWithWait` 在对方刚上线（conn 尚未就绪）时是否可靠送达。
4. **密钥方案**：bollfile 自建 `~/.bollfile/p2p-secret-{role}.json`（照搬 `p2p-secret.ts` 逻辑），用于身份稳定 + 消息签名。

---

## 8. 风险与待验证项

- **公共 DHT 隐私**：默认 topic 公开。必须用 `roomCode → sha256` 派生**私密 topic**，否则任何人都可能连进来。房间码即"共享秘密"。
- **依赖公共 bootstrap**：hyperswarm 默认连公共 DHT bootstrap 节点；若这些节点不可达，发现会失败。缓解：P6 可内置/自托管 bootstrap，或改 libp2p 自有 DHT。
- **公网入站端口**：即使双公网，hyperswarm `listen()` 需本地入站端口被放行；云厂商安全组若拦 TCP，会连不上。验证时确认入站放行（或让一方主动 `joinPeer` 拨出，hyperswarm 会自动尝试双向）。
- **粘包/分帧**：字节流必须 length-prefix 帧，否则大文件/连续消息无法切分（已在 §4.1 规定）。
- **不改 bolloon 原则**：bollfile 是独立项目，仅**参考/复制** bolloon 实现，不修改 bolloon 源码。

---

## 9. 下一步

- [x] 确认 bollfile 用 **Electron**。
- [x] 确认零配置方案：**hyperswarm 私密 topic（房间码派生）+ 自动发现**，无需用户配 IP/端口。
- [x] 进入 P1：初始化 bollfile 项目（Electron 骨架）+ 引入 hyperswarm + 实现 `HyperswarmTransport`（房间码发现 + 帧封装 + 可靠单发）。

---

## 10. 实现状态（截至 2026-09-23，已全部落地并测试通过）

### 已完成
| 阶段 | 产物 | 验证 |
|---|---|---|
| P0 项目骨架 | `package.json` / `tsconfig` / `modules.d.ts` | `tsc` 编译通过 |
| P1 传输+发现 | `transport.ts`（HyperswarmTransport：房间码派生日opic、自动发现、长度帧、sendToWithWait）+ `framing.ts` + `keys.ts`（持久身份） | CLI 冒烟：真实 hyperswarm 启动监听 + 加入房间成功 |
| P2 聊天消息 | `chat.ts`：presence 广播、消息、送达/已读回执、历史落盘、Ed25519 签名验签 | 集成测试通过 |
| P3 文件协议 | `filetransfer.ts`：offer/accept/chunk/done/resume + 逐块 sha256 + 整文件校验 + 续传 | 集成测试通过（2MB 文件 + 断点续传完整性） |
| P4 Electron UI | `main.ts` + `preload.ts` + `renderer/`（**React** 对话框：房间码页、消息气泡、文件卡片+进度条、同伴列表、历史） | `tsc` + `esbuild` 构建通过；GUI 启动需正常安装 electron 二进制 + 显示器 |
| P5 身份与配对 | `keys.ts` 稳定 keyPair + `chat.ts` 签名/验签 + 昵称 | 集成测试覆盖签名消息 |
| 辅助 | `cli.ts` 命令行驱动（无显示器实测，支持 `--local-bootstrap`）+ README | CLI 启动通过 |
| P6 本地 DHT bootstrap | `local-bootstrap.ts` + `HyperswarmTransport.useLocalBootstrap` + CLI `--local-bootstrap`（内网/离线零配置增强） | 单元测试通过；端到端 best-effort（沙箱 offline firewall 时 skip） |

### 测试结论（`npm test`）
- `integration.test.ts` — 确定性端到端，**通过**（LoopbackTransport 走真实协议：连接/presence/聊天签名/文件 sha256/续传）。
- `hyperswarm.test.ts` — 真实公共 DHT 端到端，沙箱出网受限时**自动 skip**（真实公网两台机器会跑通）。
- `lan.test.ts` — 本地 bootstrap：单元**通过**；端到端 best-effort，本沙箱 hyperdht offline firewall 时**自动 skip**（真实两机 LAN / 有公网出口内网会跑通）。

### 偏差（相对原方案）
- UI 已换 **React 19 + esbuild**（最初计划 React，中间用原生 JS 验证，现已回归 React），无需额外打包器配置。
- 构建跳过 electron 二进制（`ELECTRON_SKIP_BINARY_DOWNLOAD=1`），仅取类型。
- P6（libp2p/iroh 中继兜底）**部分落地**：已实现"本地 DHT bootstrap"作为内网/离线零配置增强；完全 air-gapped 的中继兜底按需再加。

### 你的下一步实测
1. 在 A、B 两台**公网**机器分别 `npm install`（正常装 electron）+ `npm run build`。
2. 任选 GUI(`npm start`) 或 CLI(`node dist/cli.js --room SECRET --name X`)；双方填**相同房间码**即自动互连。
3. 若机器在云上，放行入站 TCP（hyperswarm 监听端口）以便被拨入。
4. **纯内网/不想依赖公共 DHT**：任一侧加 `--local-bootstrap`（或 GUI 传 `useLocalBootstrap`），局域网内实例会自动以本地 bootstrap 互连。
5. **完全 air-gapped（无外网且离线 firewall）**：当前 hyperswarm/hyperdht 的 NAT 限制可能仍连不上，需 P6 的中继（libp2p/iroh relay）兜底，按需再实现。
